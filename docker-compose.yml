version: '3'

services: 
  app:
    volumes:
      - media:/media
      - db:/db.sqlite3
    build: .
    ports:
      - "8081:8081"
    command: gunicorn --bind 0.0.0.0:8081 main.wsgi

volumes:
  media:
  db:
